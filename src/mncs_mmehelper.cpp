#include <thread>
#include "mncs_mmehlelper.hpp"
namespace mncs {
void Helper::pushToMME(std::unordered_map<size_t, std::unique_ptr<BaseStation>>& ptr_to_nodes,
                       mncs::MME& mme)
{
  while (!_should_close) {}
}
void Helper::readFromMME(std::unordered_map<size_t, std::unique_ptr<BaseStation>>& ptr_to_nodes,
                         mncs::MME& mme)
{
  while (!_should_close) {}
}
}  // namespace mncs
